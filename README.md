mvn test

коммандаар тестийг ажиллуулна. Үр дүнгийн тайлан терминаль дээр харагдах ёстой.


Гэтэл ArrayIntQueue.java-ийн код дотор:

// TODO: This implementation contains **three bugs!**
2 Алдааг олж зассан

1.isEmpty() → size >= 0Үргэлж true буцааж, бүх логикийг эвддэг
2.peek() → null шалгалт байхгүй Хоосон queue дээр буруу утга буцаана
